# Copy to .env and fill in real values. Never commit .env.

DB_PASSWORD=changeme

# Generate a matching pair with: java scripts/GenerateSigningKey.java
KEYPASS_SIGNING_KEY=
KEYPASS_SIGNING_PUBLIC_KEY=
